{
    "name": "ramn-ai-backend",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "tsc",
        "deploy": "npm run build && sam deploy --guided",
        "local": "npm run build && sam local start-api --port 3001"
    },
    "dependencies": {
        "node-fetch": "^3.3.2"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.145",
        "@types/node": "^22.19.11",
        "typescript": "~5.8.2"
    }
}