
import React from 'react';

// This component is currently a placeholder for future HR-specific dashboard widgets.
// It is required to prevent build errors if imported elsewhere.
export const HRWidget: React.FC = () => {
    return null; 
};
